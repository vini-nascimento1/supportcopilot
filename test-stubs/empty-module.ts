// Empty stub aliased in place of `server-only` / `client-only` during tests.
// Those packages are import guards Next.js provides at build time; they aren't
// resolvable under vitest's plain Node runtime. See vitest.config.ts.
export {}
